module.exports = {
    data: {
        name: 'messageCreate',
        type: 'on',
        enabled: true,
        public: true
    },

    run: async (client, msg) => {
        //console.log('Give this man EXP!');
    }
}